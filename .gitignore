# Dependencies & build caches (source lives in /app)
app/node_modules
app/dist
app/.vite
node_modules

# Logs / OS
*.log
npm-debug.log*
.DS_Store

# NOTE: the compiled production site IS committed at the repo root
# (index.html, 404.html, .nojekyll, /assets, /static) so GitHub Pages can
# serve main:/root directly. Do not ignore those.
